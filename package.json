{
  "name": "pocket-city-planner-minigame",
  "version": "0.2.0",
  "private": true,
  "description": "Non-Unity WeChat mini game project for Pocket City Planner.",
  "type": "module",
  "scripts": {
    "build:wechat": "npm --prefix browser run build:wechat",
    "verify": "npm --prefix browser run build:wechat && node tools/verify-unity-scaffold.mjs --mode=scaffold",
    "verify:scaffold": "node tools/verify-unity-scaffold.mjs --mode=scaffold",
    "verify:exported": "node tools/verify-unity-scaffold.mjs --mode=exported",
    "verify:wechat": "npm --prefix browser run build:wechat && node tools/verify-unity-scaffold.mjs --mode=scaffold"
  }
}
